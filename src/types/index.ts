export interface TeamMember {
  name: string;
  title: string;
  specialties: string;
  description: string;
  quote: string;
}

export interface Service {
  title: string;
  description: string;
  items: string[];
}

export interface Testimonial {
  quote: string;
  name: string;
}